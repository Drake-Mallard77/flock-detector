# services/importer

Not yet built — Phase 3. Fetches OSM/DeFlock ALPR camera nodes via the Overpass API and seeds
`camera_sightings` (and rolled-up `deployments` where an `operator` tag maps to a known
agency), with ODbL attribution. See [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md).
