"use client";
import { FormEvent,useState } from "react";
export default function CorrectionForm({sightingId}:{sightingId:number}){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,data=new FormData(form),body={sightingId,requestType:data.get("requestType"),explanation:data.get("explanation"),sourceUrl:data.get("sourceUrl")};setMessage("Submitting request…");const r=await fetch("/api/corrections",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),result=await r.json();if(!r.ok){if(r.status===401&&result.signIn){location.href=result.signIn;return}setMessage(result.error??"Unable to submit request.");return}form.reset();setMessage("Request received. A moderator will review it before changing the public record.")}return <form className="correction-form" onSubmit={submit}><h2>Challenge or correct this record</h2><p>Use this workflow for factual corrections, disputes, or removal requests. Requests are private until reviewed.</p><label>Request type<select name="requestType" defaultValue="correction"><option value="correction">Correction</option><option value="challenge">Challenge accuracy</option><option value="removal">Removal request</option></select></label><label>Explanation<textarea name="explanation" minLength={20} maxLength={2000} rows={5} required placeholder="Explain what is inaccurate and what the record should say."/></label><label>Supporting public source <span className="optional">optional</span><input name="sourceUrl" type="url" placeholder="https://…"/></label><button className="primary" type="submit">Submit for review</button>{message&&<p className="form-result" role="status">{message}</p>}</form>}
